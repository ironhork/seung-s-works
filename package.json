{
  "name": "toeic-voca-master",
  "version": "1.0.0",
  "description": "TOEIC VOCA Study App",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d ."
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.82.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
